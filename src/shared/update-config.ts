/** GitHub Releases OTA — manifest is attached to each release as latest.json. */
export const GITHUB_REPO = 'rtsjsi/PillOpsDesk';

export const UPDATE_MANIFEST_URL = `https://github.com/${GITHUB_REPO}/releases/latest/download/latest.json`;
