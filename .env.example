# Google Drive backup (optional — required only for cloud backup feature)
# Create a Google Cloud OAuth Desktop client and paste the client ID here.
GOOGLE_OAUTH_CLIENT_ID=
# Optional. Desktop OAuth can work without a client secret when using PKCE/loopback.
GOOGLE_OAUTH_CLIENT_SECRET=
